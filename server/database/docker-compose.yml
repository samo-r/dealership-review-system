services:
  # Optional local MongoDB — use profile: docker compose --profile local up
  mongo_db:
    profiles: ["local"]
    container_name: db_container
    image: mongo:latest
    ports:
      - 27017:27017
    restart: always
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s

  # Optional local Redis — use profile: docker compose --profile local up
  redis:
    profiles: ["local"]
    container_name: redis_container
    image: redis:7-alpine
    ports:
      - 6379:6379
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  # Node data API — uses MONGODB_URI from .env (Atlas or local)
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: nodeapp
    ports:
      - "${PORT:-3030}:${PORT:-3030}"
    env_file:
      - .env
    environment:
      PORT: ${PORT:-3030}
      MONGODB_URI: ${MONGODB_URI}
      DB_NAME: ${DB_NAME}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
      SEED_ON_START: ${SEED_ON_START:-false}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      CHASSIS_ENCRYPTION_KEY: ${CHASSIS_ENCRYPTION_KEY}
    restart: always
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://127.0.0.1:3030/health', (res) => process.exit(res.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1));",
        ]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s

  sentiment:
    container_name: sentiment_container
    build:
      context: ../djangoapp/microservices
      dockerfile: Dockerfile
    environment:
      FLASK_DEBUG: ${FLASK_DEBUG:-false}
      FLASK_HOST: ${FLASK_HOST:-0.0.0.0}
      FLASK_PORT: ${FLASK_PORT:-5000}
    ports:
      - "${SENTIMENT_PORT:-5000}:5000"
    restart: always
    healthcheck:
      test:
        [
          "CMD",
          "python3",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5000/')",
        ]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 25s

  sentiment_worker:
    container_name: sentiment_worker_container
    build:
      context: ../djangoapp/microservices
      dockerfile: worker.Dockerfile
    env_file:
      - .env
    environment:
      REDIS_URL: ${REDIS_URL}
      SENTIMENT_QUEUE_NAME: ${SENTIMENT_QUEUE_NAME:-review_sentiment_queue}
      backend_url: http://api:3030
      sentiment_analyzer_url: http://sentiment:5000
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    restart: always
    depends_on:
      api:
        condition: service_healthy
      sentiment:
        condition: service_healthy

volumes:
  mongo_data: {}
